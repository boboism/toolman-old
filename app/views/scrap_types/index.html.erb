<h1>Listing scrap_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @scrap_types.each do |scrap_type| %>
  <tr>
    <td><%= scrap_type.name %></td>
    <td><%= scrap_type.description %></td>
    <td><%= link_to 'Show', scrap_type %></td>
    <td><%= link_to 'Edit', edit_scrap_type_path(scrap_type) %></td>
    <td><%= link_to 'Destroy', scrap_type, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Scrap type', new_scrap_type_path %>
